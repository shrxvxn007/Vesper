# Numerical / data
numpy>=1.24
pandas>=2.0
scipy>=1.11
# Parquet engine (pandas.to_parquet / read_parquet). Required by the
# synthetic generator and the integration tests; not a transitive dep
# of pandas on all platforms, so we pin it explicitly.
pyarrow>=14.0

# NLP / feature extraction
scikit-learn>=1.3
beautifulsoup4>=4.12
lxml>=4.9

# Graph
networkx>=3.0

# SEC scraping
requests>=2.31

# Convex optimization
cvxpy>=1.4

# Validation / typing
pydantic>=2.0

# Optional ML (kept optional — `scikit-learn`'s Ridge is the default)
xgboost>=2.0; python_version >= "3.9"

# Testing
pytest>=7.4
pytest-cov>=4.1

# Plotting + notebook execution (notebooks/evaluation.ipynb + CI)
matplotlib>=3.8
nbconvert>=7.0
ipykernel>=6.0
nbformat>=5.9
